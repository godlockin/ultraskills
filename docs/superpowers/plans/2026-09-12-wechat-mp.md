# wechat-mp Skill 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建 `community/wechat-mp/` skill,4 层架构全量覆盖微信公众号 ~116 个服务端 API。

**Architecture:** L0 认证(stable_token+缓存) → L1 通用网关(`raw` 任意端点直达) → L2 端点注册表(`endpoints.json`+`call`/`list`) → L3 便利命令(`doctor`/`publish`/`draft`/`material`/`comment`)。

**Tech Stack:** Python 3 纯标准库 (argparse/json/urllib),unittest 测试,零第三方依赖。

**Spec:** `docs/superpowers/specs/2026-09-12-wechat-mp-skill-design.md`

## Global Constraints

- Python 3 纯标准库,禁止 requests/pydantic 等第三方依赖
- exit code: `0` 成功 / `1` API 错误 (errcode 在 JSON 输出里,不作 exit code) / `2` 网络异常
- Secret 与 token 永不打印到 stdout/stderr;`--verbose` 时脱敏为 `****`
- destructive 端点(注册表标记)强制 `--yes`,缺省拒绝并列出将执行的操作
- 凭证查找顺序: 环境变量 `WECHAT_APP_ID`/`WECHAT_APP_SECRET` → `.wechat-mp/.env`
- token 缓存 `.wechat-mp/token.json`,过期前 300s 视为过期;`40001/42001` force_refresh 重试一次,只一次
- 所有 JSON 输出 pretty-print (`indent=2, ensure_ascii=False`)

## File Structure

```
community/wechat-mp/
├── SKILL.md                       # Task 1 骨架, Task 17 定稿
├── scripts/
│   ├── wechat_mp.py               # 单入口 CLI (L0-L3 全部)
│   └── endpoints.json             # L2 注册表 (Task 8 起)
├── references/
│   ├── api-catalog.md             # Task 17
│   └── errors.md                  # Task 17
├── examples/
│   ├── publish-article.md         # Task 17
│   └── manage-drafts.md           # Task 17
└── tests/
    └── test_wechat_mp.py          # 全部单测
```

`wechat_mp.py` 内部模块划分(单文件,按区块注释分隔): config / token / http / gateway / registry / cli。

---

### Task 1: Skill 脚手架

**Files:**
- Create: `community/wechat-mp/SKILL.md`
- Create: `community/wechat-mp/scripts/wechat_mp.py` (空骨架)
- Create: `community/wechat-mp/tests/test_wechat_mp.py` (空骨架)
- Create: `community/wechat-mp/.gitignore`

**Interfaces:**
- Produces: 目录结构与可 import 的 `wechat_mp` 模块,后续任务在此填充

- [ ] **Step 1: 创建目录与骨架文件**

`SKILL.md`:
```markdown
---
name: wechat-mp
description: 微信公众号全量 API skill。通用网关 raw/call 直达全部 ~116 个服务端接口(素材、草稿、发布、留言、菜单、用户、客服、群发、数据统计等),并内置 doctor 自检与 publish 一键发文链路。当用户提到"微信公众号接口"、"公众号素材/草稿/发布/留言/菜单/群发/数据统计"时使用。
version: 0.1.0
tags: [wechat, weixin, 公众号, api]
---

# wechat-mp — 微信公众号全量 API

(实现中,用法文档在 Task 17 定稿)
```

`scripts/wechat_mp.py`:
```python
#!/usr/bin/env python3
"""wechat-mp: 微信公众号全量 API CLI (纯标准库)."""
```

`tests/test_wechat_mp.py`:
```python
"""wechat_mp 单测。运行: python3 -m unittest discover -s community/wechat-mp/tests -v"""
import sys, os, unittest
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "scripts"))
import wechat_mp  # noqa: E402
```

`community/wechat-mp/.gitignore`:
```
.wechat-mp/
```

- [ ] **Step 2: 验证骨架可 import**

Run: `python3 -c "import sys; sys.path.insert(0,'community/wechat-mp/scripts'); import wechat_mp; print('ok')"`
Expected: `ok`

- [ ] **Step 3: Commit**

```bash
git add community/wechat-mp/
git commit -m "feat(wechat-mp): scaffold skill structure"
```

---

### Task 2: 配置加载 (config)

**Files:**
- Modify: `community/wechat-mp/scripts/wechat_mp.py`
- Test: `community/wechat-mp/tests/test_wechat_mp.py`

**Interfaces:**
- Produces: `load_config() -> tuple[str|None, str|None]` (app_id, secret);常量 `CONFIG_DIR = ".wechat-mp"`

- [ ] **Step 1: 写失败测试**

追加到 `tests/test_wechat_mp.py`:
```python
import tempfile, unittest.mock as mock

class TestLoadConfig(unittest.TestCase):
    def test_env_vars_win(self):
        with mock.patch.dict(os.environ, {"WECHAT_APP_ID": "id1", "WECHAT_APP_SECRET": "s1"}):
            self.assertEqual(wechat_mp.load_config(), ("id1", "s1"))

    def test_env_file_fallback(self):
        with tempfile.TemporaryDirectory() as d:
            envf = os.path.join(d, ".env")
            with open(envf, "w") as f:
                f.write("WECHAT_APP_ID=id2\n# comment\nWECHAT_APP_SECRET=s2\n")
            with mock.patch.dict(os.environ, {}, clear=False), \
                 mock.patch.object(wechat_mp.os.environ, "pop", side_effect=KeyError):
                pass  # 用下一种方式清环境更直接
            with mock.patch.object(wechat_mp, "CONFIG_DIR", d), \
                 mock.patch.dict(os.environ, {}, clear=True):
                self.assertEqual(wechat_mp.load_config(), ("id2", "s2"))

    def test_missing_returns_none(self):
        with mock.patch.object(wechat_mp, "CONFIG_DIR", "/nonexistent"), \
             mock.patch.dict(os.environ, {}, clear=True):
            self.assertEqual(wechat_mp.load_config(), (None, None))
```

- [ ] **Step 2: 运行确认失败**

Run: `python3 -m unittest community.wechat-mp.tests 2>/dev/null || python3 -m unittest discover -s community/wechat-mp/tests -v 2>&1 | tail -5`
Expected: FAIL/ERROR (`load_config` 不存在)

- [ ] **Step 3: 实现**

追加到 `wechat_mp.py`:
```python
import os

CONFIG_DIR = ".wechat-mp"

def load_config():
    """返回 (app_id, secret)。env 优先,回退 .wechat-mp/.env。"""
    app_id = os.environ.get("WECHAT_APP_ID")
    secret = os.environ.get("WECHAT_APP_SECRET")
    env_path = os.path.join(CONFIG_DIR, ".env")
    if app_id is None or secret is None:
        if os.path.isfile(env_path):
            data = {}
            with open(env_path, encoding="utf-8") as f:
                for line in f:
                    line = line.strip()
                    if line and not line.startswith("#") and "=" in line:
                        k, v = line.split("=", 1)
                        data[k.strip()] = v.strip()
            app_id = app_id or data.get("WECHAT_APP_ID")
            secret = secret or data.get("WECHAT_APP_SECRET")
    return app_id, secret
```

- [ ] **Step 4: 测试通过**

Run: `python3 -m unittest discover -s community/wechat-mp/tests -v`
Expected: 3 tests PASS

- [ ] **Step 5: Commit**

```bash
git add community/wechat-mp/
git commit -m "feat(wechat-mp): config loader with env + .env fallback"
```

---

### Task 3: token 缓存读写 (纯函数 + fake clock)

**Files:**
- Modify: `community/wechat-mp/scripts/wechat_mp.py`
- Test: `community/wechat-mp/tests/test_wechat_mp.py`

**Interfaces:**
- Produces: `read_token_cache(now) -> str|None`;`write_token_cache(token, expires_in, now) -> None`;`TOKEN_FILE` 常量(依赖 `CONFIG_DIR`,可 mock)

- [ ] **Step 1: 写失败测试**

```python
class TestTokenCache(unittest.TestCase):
    def test_write_read_roundtrip_and_expiry(self):
        with tempfile.TemporaryDirectory() as d:
            with mock.patch.object(wechat_mp, "CONFIG_DIR", d):
                wechat_mp.TOKEN_FILE = os.path.join(d, "token.json")
                wechat_mp.write_token_cache("T", 7200, now=1000)
                self.assertEqual(wechat_mp.read_token_cache(now=7000), "T")   # 1000+7200-300=7900
                self.assertIsNone(wechat_mp.read_token_cache(now=7900))      # 过期
                self.assertIsNone(wechat_mp.read_token_cache(now=100))       # 未来时间异常视为无效
```

- [ ] **Step 2: 运行确认失败** — Run 同 Task 2 Step 2,Expected FAIL

- [ ] **Step 3: 实现**

```python
import json, time

TOKEN_FILE = os.path.join(CONFIG_DIR, "token.json")

def write_token_cache(token, expires_in, now=None):
    if now is None:
        now = time.time()
    os.makedirs(os.path.dirname(TOKEN_FILE), exist_ok=True)
    with open(TOKEN_FILE, "w", encoding="utf-8") as f:
        json.dump({"token": token, "expires_at": now + expires_in - 300}, f)

def read_token_cache(now=None):
    if now is None:
        now = time.time()
    try:
        with open(TOKEN_FILE, encoding="utf-8") as f:
            cached = json.load(f)
    except (OSError, ValueError):
        return None
    if not isinstance(cached, dict):
        return None
    token, expires_at = cached.get("token"), cached.get("expires_at")
    if not token or not isinstance(expires_at, (int, float)) or now >= expires_at or expires_at - now > 7200:
        return None  # 过期或异常(倒退超过一个周期)
    return token
```

- [ ] **Step 4: 测试通过** — Run 同上,Expected PASS

- [ ] **Step 5: Commit**

```bash
git add community/wechat-mp/
git commit -m "feat(wechat-mp): token cache with 300s safety margin"
```

---

### Task 4: HTTP 基础 + fetch_stable_token + get_access_token

**Files:**
- Modify: `community/wechat-mp/scripts/wechat_mp.py`
- Test: `community/wechat-mp/tests/test_wechat_mp.py`

**Interfaces:**
- Produces:
  - `http_json(method, path, query=None, body=None) -> dict` — 不附 token 的底层调用,网络异常抛 `WechatNetworkError`(含原始 errmsg)
  - `class WechatNetworkError(Exception)`
  - `fetch_stable_token(app_id, secret, force_refresh=False) -> tuple[str, int]` — (token, expires_in)
  - `get_access_token(force_refresh=False) -> str` — 缓存→远端;缺凭证抛 `SystemExit(2)` 并打印缺什么(不打印值)

- [ ] **Step 1: 写失败测试**

```python
import urllib.request

class FakeResp:
    def __init__(self, body): self._b = body.encode() if isinstance(body, str) else body
    def read(self): return self._b
    def __enter__(self): return self
    def __exit__(self, *a): return False

def urlopen_200(payload):
    return mock.patch("urllib.request.urlopen", return_value=FakeResp(json.dumps(payload)))

class TestTokenFetch(unittest.TestCase):
    def test_fetch_stable_token(self):
        with urlopen_200({"access_token": "T1", "expires_in": 7200}) as m:
            tok, exp = wechat_mp.fetch_stable_token("id", "sec")
            self.assertEqual((tok, exp), ("T1", 7200))
            req = m.call_args[0][0]
            self.assertIn("/cgi-bin/stable_token", req.full_url)
            self.assertEqual(req.get_method(), "POST")

    def test_fetch_error_raises_for_caller(self):
        with urlopen_200({"errcode": 40125, "errmsg": "invalid secret"}):
            with self.assertRaises(wechat_mp.WechatApiError) as cm:
                wechat_mp.fetch_stable_token("id", "bad")
            self.assertEqual(cm.exception.payload["errcode"], 40125)

    def test_get_access_token_uses_cache(self):
        with tempfile.TemporaryDirectory() as d:
            with mock.patch.object(wechat_mp, "CONFIG_DIR", d):
                wechat_mp.TOKEN_FILE = os.path.join(d, "token.json")
                wechat_mp.write_token_cache("Cached", 7200, now=time.time())
                with mock.patch.object(wechat_mp, "fetch_stable_token") as fs:
                    self.assertEqual(wechat_mp.get_access_token(), "Cached")
                    fs.assert_not_called()
```

文件顶部 import 补 `import json, time`。

- [ ] **Step 2: 运行确认失败** — Expected FAIL

- [ ] **Step 3: 实现**

```python
import urllib.request, urllib.error, urllib.parse

API_BASE = "https://api.weixin.qq.com"

class WechatApiError(Exception):
    """服务端返回 errcode != 0。payload 为原始 JSON dict。"""
    def __init__(self, payload):
        self.payload = payload
        super().__init__(payload.get("errmsg", "unknown"))

class WechatNetworkError(Exception):
    pass

def http_json(method, path, query=None, body=None):
    """底层 HTTP: 返回解析后的 dict。errcode!=0 抛 WechatApiError,网络问题抛 WechatNetworkError。"""
    url = API_BASE + path
    if query:
        url += "?" + urllib.parse.urlencode(query)
    data = json.dumps(body).encode() if body is not None else None
    req = urllib.request.Request(url, data=data, method=method)
    if data is not None:
        req.add_header("Content-Type", "application/json")
    try:
        with urllib.request.urlopen(req, timeout=30) as resp:
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        try:
            return json.loads(e.read().decode("utf-8"))  # 微信 5xx 也回 JSON
        except ValueError:
            raise WechatNetworkError(f"HTTP {e.code}") from e
    except (urllib.error.URLError, TimeoutError, OSError) as e:
        raise WechatNetworkError(str(e)) from e

def fetch_stable_token(app_id, secret, force_refresh=False):
    body = {"grant_type": "client_credential", "appid": app_id, "secret": secret,
            "force_refresh": force_refresh}
    payload = http_json("POST", "/cgi-bin/stable_token", body=body)
    if payload.get("errcode", 0) != 0:
        raise WechatApiError(payload)
    return payload["access_token"], payload["expires_in"]

def get_access_token(force_refresh=False):
    if not force_refresh:
        cached = read_token_cache()
        if cached:
            return cached
    app_id, secret = load_config()
    if not app_id or not secret:
        missing = "WECHAT_APP_ID" if not app_id else "WECHAT_APP_SECRET"
        print(json.dumps({"errcode": -1, "errmsg": f"missing credential: {missing} "
                          f"(set env or .wechat-mp/.env)"}, ensure_ascii=False))
        raise SystemExit(2)
    token, expires_in = fetch_stable_token(app_id, secret, force_refresh)
    write_token_cache(token, expires_in)
    return token
```

- [ ] **Step 4: 测试通过** — Run 同上,Expected PASS

- [ ] **Step 5: Commit**

```bash
git add community/wechat-mp/
git commit -m "feat(wechat-mp): http_json + stable_token fetch with cache integration"
```

---

### Task 5: multipart 构造 (纯函数)

**Files:**
- Modify: `community/wechat-mp/scripts/wechat_mp.py`
- Test: `community/wechat-mp/tests/test_wechat_mp.py`

**Interfaces:**
- Produces: `build_multipart(fields: dict, file_field: str, filename: str, content: bytes, file_mime="application/octet-stream") -> tuple[bytes, str]` — (body, content_type)

- [ ] **Step 1: 写失败测试**

```python
class TestMultipart(unittest.TestCase):
    def test_build_contains_fields_and_file(self):
        body, ctype = wechat_mp.build_multipart(
            {"type": "image", "description": "封面"}, "media", "cover.png", b"\x89PNG...")
        self.assertIn("multipart/form-data", ctype)
        self.assertIn(b'name="type"', body)
        self.assertIn(b"image", body)
        self.assertIn(b'name="media"; filename="cover.png"', body)
        self.assertIn(b"\x89PNG...", body)
        # boundary 在 ctype 与 body 一致
        b = ctype.split("boundary=")[1].encode()
        self.assertIn(b, body)
```

- [ ] **Step 2: 运行确认失败** — Expected FAIL

- [ ] **Step 3: 实现**

```python
def build_multipart(fields, file_field, filename, content, file_mime="application/octet-stream"):
    boundary = "----wechatmp" + uuid.uuid4().hex
    parts = []
    for k, v in fields.items():
        parts.append(f'--{boundary}\r\nContent-Disposition: form-data; name="{k}"\r\n\r\n{v}\r\n'.encode())
    parts.append(
        f'--{boundary}\r\nContent-Disposition: form-data; name="{file_field}"; filename="{filename}"\r\n'
        f'Content-Type: {file_mime}\r\n\r\n'.encode() + content + b"\r\n")
    parts.append(f"--{boundary}--\r\n".encode())
    return b"".join(parts), f"multipart/form-data; boundary={boundary}"
```

(顶部 import `uuid`)

- [ ] **Step 4: 测试通过** — Expected PASS

- [ ] **Step 5: Commit**

```bash
git add community/wechat-mp/
git commit -m "feat(wechat-mp): multipart builder for media upload"
```

---

### Task 6: 网关 api_call (附 token + 单次重试 + 文件上传)

**Files:**
- Modify: `community/wechat-mp/scripts/wechat_mp.py`
- Test: `community/wechat-mp/tests/test_wechat_mp.py`

**Interfaces:**
- Produces: `api_call(method, path, body=None, file=None, file_field="media", extra_query=None, token=None) -> dict`
  - 自动附 `access_token` query 参数;`file` 给定时走 multipart(此时 `body` 作为普通表单字段)
  - errcode `40001/42001` 时 force_refresh 重取 token 重试一次,仍失败则返回最终 payload
  - 返回服务端 dict(不抛 WechatApiError,由 CLI 层决定 exit code)
- Consumes: `http_json`, `build_multipart`, `get_access_token` (mock 这些)

- [ ] **Step 1: 写失败测试**

```python
class FakeUrllibRequest:
    """捕获 urlopen 的 Request,按脚本顺序回放响应。"""
    def __init__(self, responses):
        self.responses = list(responses)
        self.requests = []
    def __call__(self, req, timeout=None):
        self.requests.append(req)
        r = self.responses.pop(0)
        if isinstance(r, Exception):
            raise r
        return FakeResp(json.dumps(r))

class TestApiCall(unittest.TestCase):
    def setUp(self):
        self.fx = FakeUrllibRequest([{"access_token": "T0", "expires_in": 7200}])
        self._t = mock.patch.object(wechat_mp, "TOKEN_FILE", "/nonexistent/token.json")
        self._t.start(); self.addCleanup(self._t.stop)
        self._c = mock.patch.object(wechat_mp, "load_config", return_value=("id", "sec"))
        self._c.start(); self.addCleanup(self._c.stop)

    def test_token_attached(self):
        fx = FakeUrllibRequest([{"access_token": "T0", "expires_in": 7200},
                                {"errcode": 0, "data": "ok"}])
        with mock.patch("urllib.request.urlopen", fx):
            out = wechat_mp.api_call("GET", "/cgi-bin/draft/count")
        self.assertEqual(out["data"], "ok")
        self.assertIn("access_token=T0", fx.requests[1].full_url)

    def test_retry_once_on_40001(self):
        fx = FakeUrllibRequest([{"access_token": "T0", "expires_in": 7200},
                                {"errcode": 40001, "errmsg": "invalid"},
                                {"access_token": "T1", "expires_in": 7200},
                                {"errcode": 0}])
        with mock.patch("urllib.request.urlopen", fx), \
             mock.patch.object(wechat_mp, "CONFIG_DIR", tempfile.mkdtemp()):
            wechat_mp.TOKEN_FILE = os.path.join(wechat_mp.CONFIG_DIR, "token.json")
            out = wechat_mp.api_call("GET", "/cgi-bin/draft/count")
        self.assertEqual(out["errcode"], 0)
        self.assertEqual(len(fx.requests), 4)  # 取token + 失败 + 强刷token + 重试

    def test_no_retry_twice(self):
        fx = FakeUrllibRequest([{"access_token": "T0", "expires_in": 7200},
                                {"errcode": 40001, "errmsg": "invalid"},
                                {"access_token": "T1", "expires_in": 7200},
                                {"errcode": 40001, "errmsg": "still invalid"}])
        with mock.patch("urllib.request.urlopen", fx):
            out = wechat_mp.api_call("GET", "/cgi-bin/draft/count")
        self.assertEqual(out["errcode"], 40001)
        self.assertEqual(len(fx.requests), 4)

    def test_multipart_upload(self):
        fx = FakeUrllibRequest([{"access_token": "T0", "expires_in": 7200},
                                {"errcode": 0, "media_id": "M1"}])
        with mock.patch("urllib.request.urlopen", fx):
            out = wechat_mp.api_call("POST", "/cgi-bin/material/add_material",
                                     extra_query={"type": "image"},
                                     file=("cover.png", b"\x89PNG"))
        self.assertEqual(out["media_id"], "M1")
        req = fx.requests[1]
        self.assertIn("multipart/form-data", req.headers.get("Content-type", ""))
```

- [ ] **Step 2: 运行确认失败** — Expected FAIL

- [ ] **Step 3: 实现**

```python
def _upload_multipart(url, fields, file_field, filename, content):
    body, ctype = build_multipart(fields, file_field, filename, content)
    req = urllib.request.Request(url, data=body, method="POST")
    req.add_header("Content-Type", ctype)
    try:
        with urllib.request.urlopen(req, timeout=60) as resp:
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        try:
            return json.loads(e.read().decode("utf-8"))
        except ValueError:
            raise WechatNetworkError(f"HTTP {e.code}") from e
    except (urllib.error.URLError, TimeoutError, OSError) as e:
        raise WechatNetworkError(str(e)) from e

RETRYABLE_CODES = {40001, 42001}

def api_call(method, path, body=None, file=None, file_field="media",
             extra_query=None, token=None):
    """网关: 附 token 调任意端点。file=(filename, bytes)。返回服务端 dict。"""
    for attempt in (0, 1):  # 最多两次
        if token is None:
            token = get_access_token(force_refresh=attempt == 1)
        query = dict(extra_query or {})
        query["access_token"] = token
        url = API_BASE + path + "?" + urllib.parse.urlencode(query)
        try:
            if file is not None:
                payload = _upload_multipart(url, body or {}, file_field, file[0], file[1])
            else:
                payload = http_json(method, path, query=query, body=body)
        except WechatNetworkError:
            raise
        if payload.get("errcode", 0) in RETRYABLE_CODES and attempt == 0:
            token = None
            continue
        return payload
```

注意: `http_json` 的 query 含 token 时 url 会拼一次,这里直接复用 `http_json(method, path, query=query, body=body)`。

- [ ] **Step 4: 测试通过** — Expected PASS (4 tests)

- [ ] **Step 5: Commit**

```bash
git add community/wechat-mp/
git commit -m "feat(wechat-mp): api_call gateway with token attach + single retry on 40001/42001"
```

---

### Task 7: CLI 骨架 + `raw` 子命令

**Files:**
- Modify: `community/wechat-mp/scripts/wechat_mp.py`
- Test: `community/wechat-mp/tests/test_wechat_mp.py`

**Interfaces:**
- Produces: `main(argv=None) -> int` (exit code);`raw` 用法:
  ```
  wechat_mp.py raw GET /cgi-bin/draft/count
  wechat_mp.py raw POST /cgi-bin/draft/add --data @article.json
  wechat_mp.py raw POST /cgi-bin/material/add_material?type=image --file cover.png
  ```
  `--data` 支持 `@file.json` 或内联 JSON;stdout 打印 pretty JSON;API 错误 exit 1、网络错误 exit 2。路径后可带 `?a=b` 作为 extra_query。

- [ ] **Step 1: 写失败测试**

```python
class TestRawCli(unittest.TestCase):
    def run_raw(self, argv, responses):
        fx = FakeUrllibRequest(responses)
        with mock.patch("urllib.request.urlopen", fx), \
             mock.patch.object(wechat_mp, "load_config", return_value=("id", "sec")):
            code = wechat_mp.main(argv)
        return code, fx

    def test_raw_get_ok(self):
        code, _ = self.run_raw(
            ["raw", "GET", "/cgi-bin/draft/count"],
            [{"access_token": "T", "expires_in": 7200}, {"errcode": 0, "total_count": 3}])
        self.assertEqual(code, 0)

    def test_raw_api_error_exit_1(self):
        code, _ = self.run_raw(
            ["raw", "GET", "/cgi-bin/draft/count"],
            [{"access_token": "T", "expires_in": 7200}, {"errcode": 40013, "errmsg": "invalid appid"}])
        self.assertEqual(code, 1)

    def test_raw_post_data_inline(self):
        code, fx = self.run_raw(
            ["raw", "POST", "/cgi-bin/draft/add", "--data", '{"title":"t"}'],
            [{"access_token": "T", "expires_in": 7200}, {"errcode": 0, "media_id": "M"}])
        self.assertEqual(code, 0)
        sent = json.loads(fx.requests[1].data.decode())
        self.assertEqual(sent["title"], "t")

    def test_raw_query_in_path(self):
        code, fx = self.run_raw(
            ["raw", "POST", "/cgi-bin/material/add_material?type=image", "--file", "cover.png"],
            [{"access_token": "T", "expires_in": 7200}, {"errcode": 0, "media_id": "M"}])
        self.assertEqual(code, 0)
        self.assertIn("type=image", fx.requests[1].full_url)

    def test_network_error_exit_2(self):
        fx = FakeUrllibRequest([{"access_token": "T", "expires_in": 7200},
                                urllib.error.URLError("boom")])
        with mock.patch("urllib.request.urlopen", fx), \
             mock.patch.object(wechat_mp, "load_config", return_value=("id", "sec")):
            code = wechat_mp.main(["raw", "GET", "/cgi-bin/draft/count"])
        self.assertEqual(code, 2)
```

- [ ] **Step 2: 运行确认失败** — Expected FAIL

- [ ] **Step 3: 实现**

```python
import argparse, sys

def _emit(payload):
    print(json.dumps(payload, indent=2, ensure_ascii=False))

def _load_data_arg(val):
    if val is None:
        return None
    if val.startswith("@"):
        with open(val[1:], encoding="utf-8") as f:
            return json.load(f)
    return json.loads(val)

def _split_path(path):
    if "?" in path:
        p, q = path.split("?", 1)
        extra = dict(urllib.parse.parse_qsl(q))
        return p, extra
    return path, {}

def cmd_raw(args):
    path, extra = _split_path(args.path)
    try:
        payload = api_call(args.method, path,
                           body=_load_data_arg(args.data),
                           file=_read_file(args.file) if args.file else None)
    except WechatNetworkError as e:
        _emit({"errcode": -2, "errmsg": f"network error: {e}", "rid": None})
        return 2
    _emit(payload)
    return 0 if payload.get("errcode", 0) == 0 else 1

def _read_file(path):
    with open(path, "rb") as f:
        return (os.path.basename(path), f.read())

def build_parser():
    p = argparse.ArgumentParser(prog="wechat_mp.py", description="微信公众号全量 API CLI")
    sub = p.add_subparsers(dest="command", required=True)
    raw = sub.add_parser("raw", help="通用网关: 任意端点直达")
    raw.add_argument("method", choices=["GET", "POST"])
    raw.add_argument("path", help="API 路径,可带 ?query")
    raw.add_argument("--data", help="JSON body,@file 读文件")
    raw.add_argument("--file", help="上传文件路径(multipart)")
    raw.set_defaults(func=cmd_raw)
    return p

def main(argv=None):
    args = build_parser().parse_args(argv)
    try:
        return args.func(args)
    except WechatApiError as e:
        _emit(e.payload)
        return 1
    except WechatNetworkError as e:
        _emit({"errcode": -2, "errmsg": f"network error: {e}", "rid": None})
        return 2
    except json.JSONDecodeError as e:
        print(f"invalid JSON: {e}", file=sys.stderr)
        return 2

if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 测试通过** — Expected PASS (5 tests)

- [ ] **Step 5: 手动冒烟 (无凭证,预期 exit 2 + 缺凭证提示)**

Run: `cd /tmp && python3 /path/to/repo/community/wechat-mp/scripts/wechat_mp.py raw GET /cgi-bin/draft/count; echo "exit=$?"`
Expected: 输出 missing credential JSON,exit=2

- [ ] **Step 6: Commit**

```bash
git add community/wechat-mp/
git commit -m "feat(wechat-mp): CLI skeleton + raw gateway subcommand (exit 0/1/2)"
```

---

### Task 8: 端点注册表加载 + 校验 + `list`/`call` + destructive 防护

**Files:**
- Create: `community/wechat-mp/scripts/endpoints.json` (种子条目)
- Modify: `community/wechat-mp/scripts/wechat_mp.py`
- Test: `community/wechat-mp/tests/test_wechat_mp.py`

**Interfaces:**
- Produces:
  - `load_endpoints() -> dict` (读 `endpoints.json`,与脚本同目录)
  - `validate_endpoints(registry) -> list[str]` (错误信息列表;空列表=合法)
  - `cmd_list(args)`, `cmd_call(args)`;`call` 用法: `wechat_mp.py call draft_add --data @a.json [--yes]`
  - destructive 端点缺 `--yes` → 打印将执行的操作并 exit 2
- 注册表 schema (spec §4.3): `{"<id>": {"name","method","path","category","params":[],"destructive":bool,"notes"?, "auth"?}}`,`auth` 默认 `"token"`,可 `"none"`

- [ ] **Step 1: 写种子注册表**

`scripts/endpoints.json`:
```json
{
  "draft_count": {
    "name": "获取草稿的总数", "method": "GET", "path": "/cgi-bin/draft/count",
    "category": "草稿管理", "params": [], "destructive": false
  },
  "draft_add": {
    "name": "新增草稿", "method": "POST", "path": "/cgi-bin/draft/add",
    "category": "草稿管理", "params": ["articles"], "destructive": false,
    "notes": "thumb_media_id 需先经 material/add_material 获取"
  },
  "draft_delete": {
    "name": "删除草稿", "method": "POST", "path": "/cgi-bin/draft/delete",
    "category": "草稿管理", "params": ["media_id"], "destructive": true
  },
  "stable_token": {
    "name": "获取稳定版接口调用凭据", "method": "POST", "path": "/cgi-bin/stable_token",
    "category": "基础接口", "params": ["grant_type","appid","secret","force_refresh"],
    "destructive": false, "auth": "none"
  }
}
```

- [ ] **Step 2: 写失败测试**

```python
class TestRegistry(unittest.TestCase):
    def test_validate_ok(self):
        self.assertEqual(wechat_mp.validate_endpoints(wechat_mp.load_endpoints()), [])

    def test_validate_catches_bad_entries(self):
        bad = {"x": {"name": "n", "method": "PUT", "path": "no-slash",
                     "category": "c", "params": [], "destructive": False},
               "draft_count": {"name": "n", "method": "GET", "path": "/p",
                     "category": "c", "params": [], "destructive": False}}
        errs = wechat_mp.validate_endpoints(bad)
        self.assertTrue(any("method" in e for e in errs))
        self.assertTrue(any("path" in e for e in errs))

class TestCallCmd(unittest.TestCase):
    def test_call_routes_to_path(self):
        fx = FakeUrllibRequest([{"access_token": "T", "expires_in": 7200}, {"errcode": 0, "total_count": 5}])
        with mock.patch("urllib.request.urlopen", fx), \
             mock.patch.object(wechat_mp, "load_config", return_value=("id", "sec")):
            code = wechat_mp.main(["call", "draft_count"])
        self.assertEqual(code, 0)
        self.assertIn("/cgi-bin/draft/count", fx.requests[1].full_url.split("?")[0])

    def test_call_unknown_id(self):
        code = wechat_mp.main(["call", "nope"])
        self.assertEqual(code, 2)

    def test_call_destructive_requires_yes(self):
        code = wechat_mp.main(["call", "draft_delete", "--data", '{"media_id":"M"}'])
        self.assertEqual(code, 2)

    def test_call_destructive_with_yes(self):
        fx = FakeUrllibRequest([{"access_token": "T", "expires_in": 7200}, {"errcode": 0}])
        with mock.patch("urllib.request.urlopen", fx), \
             mock.patch.object(wechat_mp, "load_config", return_value=("id", "sec")):
            code = wechat_mp.main(["call", "draft_delete", "--data", '{"media_id":"M"}', "--yes"])
        self.assertEqual(code, 0)

    def test_list_filters_category(self):
        import io, contextlib
        buf = io.StringIO()
        with contextlib.redirect_stdout(buf):
            wechat_mp.main(["list", "--category", "草稿管理"])
        self.assertIn("draft_add", buf.getvalue())
        self.assertNotIn("stable_token", buf.getvalue())
```

- [ ] **Step 3: 运行确认失败** — Expected FAIL

- [ ] **Step 4: 实现**

```python
ENDPOINTS_PATH = os.path.join(os.path.dirname(os.path.abspath(__file__)), "endpoints.json")

def load_endpoints():
    with open(ENDPOINTS_PATH, encoding="utf-8") as f:
        return json.load(f)

def validate_endpoints(registry):
    errs = []
    seen_paths = {}
    for eid, e in registry.items():
        if e.get("method") not in ("GET", "POST"):
            errs.append(f"{eid}: method must be GET/POST")
        if not str(e.get("path", "")).startswith("/"):
            errs.append(f"{eid}: path must start with /")
        for field in ("name", "category", "params", "destructive"):
            if field not in e:
                errs.append(f"{eid}: missing field {field}")
        if e.get("auth", "token") not in ("token", "none"):
            errs.append(f"{eid}: auth must be token/none")
        key = (e.get("method"), e.get("path"))
        if key in seen_paths:
            errs.append(f"{eid}: duplicate method+path with {seen_paths[key]}")
        seen_paths[key] = eid
    return errs

def cmd_list(args):
    registry = load_endpoints()
    errs = validate_endpoints(registry)
    if errs:
        print(json.dumps(errs, ensure_ascii=False, indent=2), file=sys.stderr)
        return 2
    rows = [{"id": k, **v} for k, v in registry.items()
             if (not args.category or v["category"] == args.category)
             and (not args.search or args.search in v["name"] or args.search in k)]
    _emit(rows)
    return 0

def cmd_call(args):
    registry = load_endpoints()
    ep = registry.get(args.endpoint)
    if ep is None:
        print(f"unknown endpoint: {args.endpoint} (run 'list' to see all)", file=sys.stderr)
        return 2
    if ep.get("destructive") and not args.yes:
        print(json.dumps({"blocked": True, "endpoint": args.endpoint,
                          "name": ep["name"], "path": ep["path"],
                          "hint": "destructive operation, add --yes to confirm"},
                         ensure_ascii=False, indent=2))
        return 2
    try:
        payload = api_call(ep["method"], ep["path"], body=_load_data_arg(args.data),
                           file=_read_file(args.file) if args.file else None)
    except WechatNetworkError as e:
        _emit({"errcode": -2, "errmsg": f"network error: {e}", "rid": None})
        return 2
    _emit(payload)
    return 0 if payload.get("errcode", 0) == 0 else 1
```

`build_parser()` 中追加:
```python
    ls = sub.add_parser("list", help="列出端点")
    ls.add_argument("--category"); ls.add_argument("--search")
    ls.set_defaults(func=cmd_list)
    call = sub.add_parser("call", help="按注册名调用端点")
    call.add_argument("endpoint")
    call.add_argument("--data"); call.add_argument("--file")
    call.add_argument("--yes", action="store_true", help="确认 destructive 操作")
    call.set_defaults(func=cmd_call)
```

- [ ] **Step 5: 测试通过** — Expected PASS

- [ ] **Step 6: Commit**

```bash
git add community/wechat-mp/
git commit -m "feat(wechat-mp): endpoint registry + list/call + destructive --yes guard"
```

---

### Task 9: 注册表批次 A — 基础/管理/菜单/杂项 (21 个)

**Files:**
- Modify: `community/wechat-mp/scripts/endpoints.json`

**Interfaces:**
- Consumes: `validate_endpoints` (Task 8)
- Produces: 注册表含下表全部条目

- [ ] **Step 1: 录入条目**

按下表逐条追加(JSON 结构同 Task 8 种子;`auth:none` 仅 token_get/stable_token;destructive 列 ✔):

| id | name | method | path | category | destructive |
|---|---|---|---|---|---|
| token_get | 获取接口调用凭据 | GET | /cgi-bin/token | 基础接口 | |
| stable_token | 获取稳定版接口调用凭据 | POST | /cgi-bin/stable_token | 基础接口 | |
| callback_check | 网络通信检测 | POST | /cgi-bin/callback/check | 基础接口 | |
| get_api_domain_ip | 获取微信API服务器IP | GET | /cgi-bin/get_api_domain_ip | 基础接口 | |
| getcallbackip | 获取微信推送服务器IP | GET | /cgi-bin/getcallbackip | 基础接口 | |
| quota_clear_all | 重置API调用次数 | POST | /cgi-bin/clear_quota | openApi管理 | ✔ |
| quota_get | 查询API调用额度 | POST | /cgi-bin/openapi/quota/get | openApi管理 | |
| rid_get | 查询rid信息 | POST | /cgi-bin/openapi/rid/get | openApi管理 | |
| quota_clear_all_v2 | 用AppSecret重置API调用次数 | POST | /cgi-bin/clear_quota/v2 | openApi管理 | ✔ |
| quota_clear | 重置指定API调用次数 | POST | /cgi-bin/openapi/quota/clear | openApi管理 | ✔ |
| menu_create | 创建自定义菜单 | POST | /cgi-bin/menu/create | 自定义菜单 | ✔ |
| selfmenu_info | 查询自定义菜单信息 | GET | /cgi-bin/get_current_selfmenu_info | 自定义菜单 | |
| menu_get | 获取自定义菜单配置 | GET | /cgi-bin/menu/get | 自定义菜单 | |
| menu_delete | 删除自定义菜单 | POST | /cgi-bin/menu/delete | 自定义菜单 | ✔ |
| menu_addconditional | 创建个性化菜单 | POST | /cgi-bin/menu/addconditional | 自定义菜单 | ✔ |
| menu_delconditional | 删除个性化菜单 | POST | /cgi-bin/menu/delconditional | 自定义菜单 | ✔ |
| menu_trymatch | 测试个性化菜单匹配结果 | POST | /cgi-bin/menu/trymatch | 自定义菜单 | |
| autoreply_info | 获取自动回复规则 | GET | /cgi-bin/get_current_autoreply_info | 自动回复 | |
| ticket_get | 获取sdk临时票据 | GET | /cgi-bin/ticket/getticket | 网页开发 | |
| subscribe_send | 发送一次性订阅消息 | POST | /cgi-bin/message/template/subscribe | 一次性订阅消息 | |
| changeopenid | 转换openid | POST | /cgi-bin/changeopenid | 用户管理 | |

注: `quota_get`/`rid_get` 官方文档为 POST body(JSON),method 按 POST 录入。

- [ ] **Step 2: 校验**

Run: `python3 -m unittest discover -s community/wechat-mp/tests -v -k TestRegistry`
Expected: PASS (validate_ok 现在覆盖 21+ 条)

Run: `python3 community/wechat-mp/scripts/wechat_mp.py list --category 基础接口 | python3 -c "import json,sys; print(len(json.load(sys.stdin)))"`
Expected: `5`

- [ ] **Step 3: Commit**

```bash
git add community/wechat-mp/scripts/endpoints.json
git commit -m "feat(wechat-mp): registry batch A — 基础/openApi/菜单/杂项 21 endpoints"
```

---

### Task 10: 注册表批次 B — 素材/草稿/商品卡片/留言/发布 (30 个)

**Files:**
- Modify: `community/wechat-mp/scripts/endpoints.json`

- [ ] **Step 1: 录入条目**

| id | name | method | path | category | destructive |
|---|---|---|---|---|---|
| material_add | 上传永久素材 | POST | /cgi-bin/material/add_material | 素材管理 | |
| material_get | 获取永久素材 | POST | /cgi-bin/material/get_material | 素材管理 | |
| material_count | 获取永久素材总数 | GET | /cgi-bin/material/get_materialcount | 素材管理 | |
| material_batchget | 获取永久素材列表 | POST | /cgi-bin/material/batchget_material | 素材管理 | |
| material_del | 删除永久素材 | POST | /cgi-bin/material/del_material | 素材管理 | ✔ |
| media_uploadimg | 上传发表内容中的图片 | POST | /cgi-bin/media/uploadimg | 素材管理 | |
| media_upload | 新增临时素材 | POST | /cgi-bin/media/upload | 素材管理 | |
| media_get | 获取临时素材 | GET | /cgi-bin/media/get | 素材管理 | |
| media_get_jssdk | 获取高清语音素材 | GET | /cgi-bin/media/get/jssdk | 素材管理 | |
| draft_switch | 草稿箱开关设置 | POST | /cgi-bin/draft/switch | 草稿管理 | ✔ |
| draft_add | 新增草稿 | POST | /cgi-bin/draft/add | 草稿管理 | |
| draft_get | 获取草稿详情 | POST | /cgi-bin/draft/get | 草稿管理 | |
| draft_delete | 删除草稿 | POST | /cgi-bin/draft/delete | 草稿管理 | ✔ |
| draft_update | 更新草稿 | POST | /cgi-bin/draft/update | 草稿管理 | |
| draft_count | 获取草稿的总数 | GET | /cgi-bin/draft/count | 草稿管理 | |
| draft_batchget | 获取草稿列表 | POST | /cgi-bin/draft/batchget | 草稿管理 | |
| product_card_info | 获取商品卡片的DOM结构 | POST | /channels/ec/service/product/getcardinfo | 商品卡片 | |
| comment_open | 打开已群发文章评论 | POST | /cgi-bin/comment/open | 留言管理 | |
| comment_close | 关闭已群发文章评论 | POST | /cgi-bin/comment/close | 留言管理 | |
| comment_list | 查看指定文章的评论数据 | POST | /cgi-bin/comment/list | 留言管理 | |
| comment_markelect | 评论标记精选 | POST | /cgi-bin/comment/markelect | 留言管理 | |
| comment_unmarkelect | 评论取消精选 | POST | /cgi-bin/comment/unmarkelect | 留言管理 | |
| comment_delete | 删除评论 | POST | /cgi-bin/comment/delete | 留言管理 | ✔ |
| comment_reply_add | 回复评论 | POST | /cgi-bin/comment/reply/add | 留言管理 | |
| comment_reply_delete | 删除回复 | POST | /cgi-bin/comment/reply/delete | 留言管理 | ✔ |
| freepublish_submit | 发布草稿 | POST | /cgi-bin/freepublish/submit | 发布能力 | ✔ |
| freepublish_get | 发布状态查询 | POST | /cgi-bin/freepublish/get | 发布能力 | |
| freepublish_delete | 删除发布文章 | POST | /cgi-bin/freepublish/delete | 发布能力 | ✔ |
| freepublish_batchget | 获取已发布的消息列表 | POST | /cgi-bin/freepublish/batchget | 发布能力 | |
| freepublish_getarticle | 获取已发布图文信息 | POST | /cgi-bin/freepublish/getarticle | 发布能力 | |

(已有 seed 条目更新为上表完整字段;`freepublish_submit` 标 destructive:✔ 因发布是外向动作,加 notes "发布后对全部粉丝可见")

- [ ] **Step 2: 校验** — 同 Task 9 Step 2,`list --category 素材管理` Expected `9`

- [ ] **Step 3: Commit**

```bash
git add community/wechat-mp/scripts/endpoints.json
git commit -m "feat(wechat-mp): registry batch B — 素材/草稿/留言/发布 30 endpoints"
```

---

### Task 11: 注册表批次 C — 用户/标签/客服 (30 个)

**Files:**
- Modify: `community/wechat-mp/scripts/endpoints.json`

- [ ] **Step 1: 录入条目**

| id | name | method | path | category | destructive |
|---|---|---|---|---|---|
| tags_get | 获取标签 | GET | /cgi-bin/tags/get | 标签管理 | |
| tags_create | 创建标签 | POST | /cgi-bin/tags/create | 标签管理 | |
| tags_update | 编辑标签 | POST | /cgi-bin/tags/update | 标签管理 | |
| tags_delete | 删除标签 | POST | /cgi-bin/tags/delete | 标签管理 | ✔ |
| user_tag_get | 获取标签下粉丝列表 | POST | /cgi-bin/user/tag/get | 标签管理 | |
| batchtagging | 批量为用户打标签 | POST | /cgi-bin/tags/members/batchtagging | 标签管理 | |
| batchuntagging | 批量为用户取消标签 | POST | /cgi-bin/tags/members/batchuntagging | 标签管理 | |
| tag_getidlist | 获取用户的标签列表 | POST | /cgi-bin/tags/getidlist | 标签管理 | |
| user_info | 获取用户基本信息 | GET | /cgi-bin/user/info | 用户信息 | |
| user_info_batchget | 批量获取用户基本信息 | POST | /cgi-bin/user/info/batchget | 用户信息 | |
| user_get | 获取关注用户列表 | GET | /cgi-bin/user/get | 用户信息 | |
| blacklist_get | 获取公众号的黑名单列表 | POST | /cgi-bin/tags/members/getblacklist | 用户信息 | |
| blacklist_batch | 拉黑用户 | POST | /cgi-bin/tags/members/batchblacklist | 用户信息 | ✔ |
| blacklist_batchun | 取消拉黑用户 | POST | /cgi-bin/tags/members/batchunblacklist | 用户信息 | |
| user_remark | 设置用户备注名 | POST | /cgi-bin/user/info/updateremark | 用户信息 | |
| kflist | 获取所有客服账号 | GET | /cgi-bin/customservice/getkflist | 客服管理 | |
| kfonlinelist | 获取在线客服列表 | GET | /cgi-bin/customservice/getonlinekflist | 客服管理 | |
| kfaccount_add | 添加客服账号 | POST | /customservice/kfaccount/add | 客服管理 | |
| kfaccount_update | 修改客服账号 | POST | /customservice/kfaccount/update | 客服管理 | |
| kfaccount_del | 删除客服账号 | POST | /customservice/kfaccount/del | 客服管理 | ✔ |
| kfaccount_invite | 邀请绑定客服账号 | POST | /customservice/kfaccount/inviteworker | 客服管理 | |
| kf_uploadheadimg | 设置客服头像 | POST | /customservice/kfaccount/uploadheadimg | 客服管理 | |
| kfsession_create | 创建会话 | POST | /customservice/kfsession/create | 会话控制 | |
| kfsession_close | 关闭会话 | POST | /customservice/kfsession/close | 会话控制 | |
| kfsession_get | 获取客户会话状态 | GET | /customservice/kfsession/getsession | 会话控制 | |
| kfsession_list | 获取客服会话列表 | GET | /customservice/kfsession/getsessionlist | 会话控制 | |
| kfsession_waitcase | 获取未接入会话列表 | GET | /customservice/kfsession/getwaitcase | 会话控制 | |
| kf_msg_send | 发送客服消息 | POST | /cgi-bin/message/custom/send | 客服消息 | |
| kf_typing | 客服输入状态 | POST | /cgi-bin/message/custom/typing | 客服消息 | |
| msg_record | 获取聊天记录 | POST | /customservice/msgrecord/getmsglist | 客服消息 | |

- [ ] **Step 2: 校验** — 同上,`list` 总数 Expected `82`(21+30+30+seed 重复已去重)

- [ ] **Step 3: Commit**

```bash
git add community/wechat-mp/scripts/endpoints.json
git commit -m "feat(wechat-mp): registry batch C — 标签/用户/客服 30 endpoints"
```

---

### Task 12: 注册表批次 D — 群发/数据统计/智能/门店/就医 (55 个)

**Files:**
- Modify: `community/wechat-mp/scripts/endpoints.json`

- [ ] **Step 1: 录入条目**

群发消息 (7):
| id | name | method | path | category | destructive |
|---|---|---|---|---|---|
| mass_sendall | 根据标签群发消息 | POST | /cgi-bin/message/mass/sendall | 群发消息 | ✔ |
| mass_delete | 删除群发消息 | POST | /cgi-bin/message/mass/delete | 群发消息 | ✔ |
| mass_preview | 预览消息 | POST | /cgi-bin/message/mass/preview | 群发消息 | |
| mass_get | 查询群发消息发送状态 | POST | /cgi-bin/message/mass/get | 群发消息 | |
| mass_speed_get | 获取群发速度 | POST | /cgi-bin/message/mass/speed/get | 群发消息 | |
| mass_speed_set | 设置群发速度 | POST | /cgi-bin/message/mass/speed/set | 群发消息 | |
| uploadnews | 上传图文消息素材(旧) | POST | /cgi-bin/media/uploadnews | 群发消息 | |

数据统计 (21,全部 POST,category 数据统计,均非 destructive,notes 标日期参数粒度 begin_date/end_date):
`getusersummary`/`getusercumulate` → /datacube/getusersummary, /datacube/getusercumulate;
`getarticlesummary` `/datacube/getarticlesummary`;`getarticletotal` `/datacube/getarticletotal`;`getuserread` `/datacube/getuserread`;`getuserreadhour` `/datacube/getuserreadhour`;`getusershare` `/datacube/getusershare`;`getusersharehour` `/datacube/getusersharehour`;`getupstreammsg` `/datacube/getupstreammsg`;`getupstreammsghour` `/datacube/getupstreammsghour`;`getupstreammsgweek` `/datacube/getupstreammsgweek`;`getupstreammsgmonth` `/datacube/getupstreammsgmonth`;`getupstreammsgdist` `/datacube/getupstreammsgdist`;`getupstreammsgdistweek` `/datacube/getupstreammsgdistweek`;`getupstreammsgdistmonth` `/datacube/getupstreammsgdistmonth`;`getarticleread` `/datacube/getarticleread`;`getarticleshare` `/datacube/getarticleshare`;`getbizsummary` `/datacube/getbizsummary`;`getarticletotaldetail` `/datacube/getarticletotaldetail`;`getinterfacesummary` `/datacube/getinterfacesummary`;`getinterfacesummaryhour` `/datacube/getinterfacesummaryhour`

智能接口 (12,category 智能接口,均非 destructive):
`voice_translate` POST /cgi-bin/media/voice/translatecontent;`voice_add` POST /cgi-bin/media/voice/addvoicetorecofortext;`voice_query` POST /cgi-bin/media/voice/queryrecoresultfortext;
`ocr_menu` POST /cv/ocr/menu;`ocr_comm` POST /cv/ocr/comm;`ocr_driving` POST /cv/ocr/driving;`ocr_bankcard` POST /cv/ocr/bankcard;`ocr_bizlicense` POST /cv/ocr/bizlicense;`ocr_drivinglicense` POST /cv/ocr/drivinglicense;`ocr_idcard` POST /cv/ocr/idcard;
`img_aicrop` POST /cv/img/aicrop;`img_qrcode` POST /cv/img/qrcode

微信门店 (12,category 微信门店,均非 destructive,method 按 path 表: POST 为主、get_store_list POST、get_district POST、search_map_poi POST):
`store_category` POST /wxa/get_merchant_category;`store_apply` POST /wxa/apply_merchant;`store_audit_info` POST /wxa/get_merchant_audit_info;`store_modify` POST /wxa/modify_merchant;`district_get` POST /wxa/get_district;`map_poi_search` POST /wxa/search_map_poi;`store_add` POST /wxa/add_store;`store_info` POST /wxa/get_store_info;`store_list` POST /wxa/get_store_list;`store_del` POST /wxa/del_store;`store_update` POST /wxa/update_store;`map_poi_create` POST /wxa/create_map_poi
(`store_del` destructive: ✔)

就医助手 (1): `cityservice_send` POST /cityservice/sendchannelmsg,category 就医助手

- [ ] **Step 2: 全量对账校验**

Run: `python3 community/wechat-mp/scripts/wechat_mp.py list | python3 -c "import json,sys; d=json.load(sys.stdin); print(len(d)); print(sorted({e['category'] for e in d}))"`
Expected: 总数 ≈116(±去重差异,≥110),22 个 category 全出现

Run: `python3 -m unittest discover -s community/wechat-mp/tests -v`
Expected: 全 PASS (validate 覆盖全表)

- [ ] **Step 3: Commit**

```bash
git add community/wechat-mp/scripts/endpoints.json
git commit -m "feat(wechat-mp): registry batch D — 群发/统计/智能/门店/就医, registry complete"
```

---

### Task 13: `doctor` 自检命令

**Files:**
- Modify: `community/wechat-mp/scripts/wechat_mp.py`
- Test: `community/wechat-mp/tests/test_wechat_mp.py`

**Interfaces:**
- Produces: `cmd_doctor(args) -> int`。检查链: ① 凭证存在 ② stable_token 可取 ③ `POST /cgi-bin/openapi/quota/get` body `{"cgi_path":"/cgi-bin/draft/count"}` 读 quota。输出分步 PASS/FAIL 报告;FAIL 时给修复提示(40164 → IP 白名单路径,40125 → 检查 secret 等)。全部通过 exit 0,否则 1。

- [ ] **Step 1: 写失败测试**

```python
class TestDoctor(unittest.TestCase):
    def test_doctor_all_pass(self):
        fx = FakeUrllibRequest([
            {"access_token": "T", "expires_in": 7200},
            {"errcode": 0, "quota": 100000, "used": 5}])
        with mock.patch("urllib.request.urlopen", fx), \
             mock.patch.object(wechat_mp, "load_config", return_value=("id", "sec")), \
             mock.patch.object(wechat_mp, "CONFIG_DIR", tempfile.mkdtemp()):
            wechat_mp.TOKEN_FILE = os.path.join(wechat_mp.CONFIG_DIR, "token.json")
            import io, contextlib
            buf = io.StringIO()
            with contextlib.redirect_stdout(buf):
                code = wechat_mp.main(["doctor"])
        self.assertEqual(code, 0)
        self.assertIn("PASS", buf.getvalue())

    def test_doctor_whitelist_hint(self):
        fx = FakeUrllibRequest([
            {"errcode": 40164, "errmsg": "invalid ip, not in whitelist"}])
        with mock.patch("urllib.request.urlopen", fx), \
             mock.patch.object(wechat_mp, "load_config", return_value=("id", "sec")), \
             mock.patch.object(wechat_mp, "CONFIG_DIR", tempfile.mkdtemp()):
            wechat_mp.TOKEN_FILE = os.path.join(wechat_mp.CONFIG_DIR, "token.json")
            import io, contextlib
            buf = io.StringIO()
            with contextlib.redirect_stdout(buf):
                code = wechat_mp.main(["doctor"])
        self.assertEqual(code, 1)
        self.assertIn("IP", buf.getvalue())

    def test_doctor_no_creds(self):
        with mock.patch.object(wechat_mp, "CONFIG_DIR", "/nonexistent"), \
             mock.patch.dict(os.environ, {}, clear=True):
            import io, contextlib
            buf = io.StringIO()
            with contextlib.redirect_stdout(buf):
                code = wechat_mp.main(["doctor"])
        self.assertEqual(code, 1)
```

- [ ] **Step 2: 运行确认失败** — Expected FAIL

- [ ] **Step 3: 实现**

```python
DOCTOR_HINTS = {
    40164: "IP 不在白名单: mp.weixin.qq.com → 设置与开发 → 基本配置 → IP白名单, 加入本机出口 IP",
    40125: "secret 无效: 检查 .wechat-mp/.env 中 WECHAT_APP_SECRET (注意大小写)",
    40013: "appid 无效: 检查 WECHAT_APP_ID",
    40243: "AppSecret 已冻结: mp.weixin.qq.com → 基本配置 → 重置后更新 .env",
}

def cmd_doctor(args):
    steps = []
    app_id, secret = load_config()
    steps.append(("credentials", app_id and secret,
                  "set WECHAT_APP_ID/WECHAT_APP_SECRET or .wechat-mp/.env"))
    if not (app_id and secret):
        return _report_doctor(steps)
    try:
        token, _ = fetch_stable_token(app_id, secret)
        steps.append(("stable_token", True, ""))
        write_token_cache(token, 7200)
        quota = http_json("POST", "/cgi-bin/openapi/quota/get",
                          query={"access_token": token},
                          body={"cgi_path": "/cgi-bin/draft/count"})
        ok = quota.get("errcode", 0) == 0
        hint = DOCTOR_HINTS.get(quota.get("errcode"), quota.get("errmsg", ""))
        steps.append(("quota", ok, hint))
    except WechatApiError as e:
        code = e.payload.get("errcode", -1)
        steps.append(("stable_token", False, DOCTOR_HINTS.get(code, e.payload.get("errmsg", ""))))
    except WechatNetworkError as e:
        steps.append(("network", False, str(e)))
    return _report_doctor(steps)

def _report_doctor(steps):
    all_ok = True
    for name, ok, hint in steps:
        mark = "PASS" if ok else "FAIL"
        if not ok:
            all_ok = False
        line = f"[{mark}] {name}" + (f" — {hint}" if hint and not ok else "")
        print(line)
    return 0 if all_ok else 1
```

`build_parser()` 追加:
```python
    doc = sub.add_parser("doctor", help="环境自检: 凭证/token/quota")
    doc.set_defaults(func=cmd_doctor)
```

注意 `fetch_stable_token` 网络异常也要接:`except WechatNetworkError` 分支已覆盖 (fetch 内部会抛)。

- [ ] **Step 4: 测试通过** — Expected PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add community/wechat-mp/
git commit -m "feat(wechat-mp): doctor self-check with errcode-specific fix hints"
```

---

### Task 14: `draft` / `material` 便利命令

**Files:**
- Modify: `community/wechat-mp/scripts/wechat_mp.py`
- Test: `community/wechat-mp/tests/test_wechat_mp.py`

**Interfaces:**
- Produces:
  - `wechat_mp.py draft ls [--offset N] [--count N]` → draft/batchget;`draft get --media-id M`;`draft count`;`draft del --media-id M [--yes]`(走 destructive guard,直接复用 registry 的 draft_delete 条目规则)
  - `wechat_mp.py material ls --type image|video|voice|news [--offset] [--count]`;`material upload --file f.png --type image [--name "说明"]`;`material del --media-id M [--yes]`
  - 内部助手 `_guarded_call(endpoint_id, body=None, file=None, yes=False, **kw) -> tuple[int, dict]`: 查 registry,destructive 且非 yes → 打印 blocked JSON 返回 (2, {}),否则 api_call

- [ ] **Step 1: 写失败测试**

```python
class TestConvenience(unittest.TestCase):
    def _run(self, argv, responses):
        fx = FakeUrllibRequest(responses)
        with mock.patch("urllib.request.urlopen", fx), \
             mock.patch.object(wechat_mp, "load_config", return_value=("id", "sec")):
            code = wechat_mp.main(argv)
        return code, fx

    def test_draft_ls(self):
        code, fx = self._run(["draft", "ls"],
            [{"access_token": "T", "expires_in": 7200},
             {"errcode": 0, "total_count": 1, "item": []}])
        self.assertEqual(code, 0)
        sent = json.loads(fx.requests[1].data.decode())
        self.assertEqual(sent, {"offset": 0, "count": 20, "no_content": 1})

    def test_draft_del_requires_yes(self):
        code, _ = self._run(["draft", "del", "--media-id", "M"],
            [{"access_token": "T", "expires_in": 7200}])
        self.assertEqual(code, 2)

    def test_material_upload(self):
        code, fx = self._run(["material", "upload", "--file", __file__, "--type", "image"],
            [{"access_token": "T", "expires_in": 7200},
             {"errcode": 0, "media_id": "M1", "url": "http://x/1"}])
        self.assertEqual(code, 0)
        self.assertIn("type=image", fx.requests[1].full_url)

    def test_material_ls_bad_type(self):
        code, _ = self._run(["material", "ls", "--type", "doc"],
            [{"access_token": "T", "expires_in": 7200}])
        self.assertEqual(code, 2)
```

- [ ] **Step 2: 运行确认失败** — Expected FAIL

- [ ] **Step 3: 实现**

```python
def _guarded_call(endpoint_id, body=None, file=None, yes=False, extra_query=None):
    ep = load_endpoints().get(endpoint_id)
    if ep.get("destructive") and not yes:
        print(json.dumps({"blocked": True, "endpoint": endpoint_id, "name": ep["name"],
                          "hint": "add --yes to confirm"}, ensure_ascii=False, indent=2))
        return 2, {}
    try:
        payload = api_call(ep["method"], ep["path"], body=body, file=file,
                           extra_query=extra_query)
    except WechatNetworkError as e:
        _emit({"errcode": -2, "errmsg": f"network error: {e}", "rid": None})
        return 2, {}
    _emit(payload)
    return (0 if payload.get("errcode", 0) == 0 else 1), payload

def cmd_draft(args):
    if args.action == "ls":
        return _guarded_call("draft_batchget", {"offset": args.offset, "count": args.count,
                                                "no_content": 1})[0]
    if args.action == "get":
        return _guarded_call("draft_get", {"media_id": args.media_id})[0]
    if args.action == "count":
        return _guarded_call("draft_count")[0]
    if args.action == "del":
        return _guarded_call("draft_delete", {"media_id": args.media_id}, yes=args.yes)[0]

def cmd_material(args):
    if args.action == "ls":
        if args.type not in ("image", "video", "voice", "news"):
            print("type must be image|video|voice|news", file=sys.stderr)
            return 2
        return _guarded_call("material_batchget",
                             {"type": args.type, "offset": args.offset,
                              "count": args.count, "no_content": 1})[0]
    if args.action == "upload":
        return _guarded_call("material_add", {"description": args.name or ""},
                             file=_read_file(args.file), yes=True,
                             extra_query={"type": args.type})[0]
    if args.action == "del":
        return _guarded_call("material_del", {"media_id": args.media_id}, yes=args.yes)[0]
```

`build_parser()` 追加:
```python
    dr = sub.add_parser("draft", help="草稿箱管理")
    dr.add_argument("action", choices=["ls", "get", "count", "del"])
    dr.add_argument("--media-id"); dr.add_argument("--offset", type=int, default=0)
    dr.add_argument("--count", type=int, default=20)
    dr.add_argument("--yes", action="store_true")
    dr.set_defaults(func=cmd_draft)
    mt = sub.add_parser("material", help="永久素材管理")
    mt.add_argument("action", choices=["ls", "upload", "del"])
    mt.add_argument("--file"); mt.add_argument("--type", default="image")
    mt.add_argument("--media-id"); mt.add_argument("--name")
    mt.add_argument("--offset", type=int, default=0); mt.add_argument("--count", type=int, default=20)
    mt.add_argument("--yes", action="store_true")
    mt.set_defaults(func=cmd_material)
```

注: `material upload` 本身非 destructive,yes=True 只是跳过 guard 无副作用;`file_field` 对 add_material 用 `media`(默认)。

- [ ] **Step 4: 测试通过** — Expected PASS

- [ ] **Step 5: Commit**

```bash
git add community/wechat-mp/
git commit -m "feat(wechat-mp): draft/material convenience commands"
```

---

### Task 15: `comment` 便利命令

**Files:**
- Modify: `community/wechat-mp/scripts/wechat_mp.py`
- Test: `community/wechat-mp/tests/test_wechat_mp.py`

**Interfaces:**
- Produces:
  - `wechat_mp.py comment ls --article-id A [--begin 0] [--count 50] [--type 0]`
  - `comment reply --article-id A --user-id U --content C` (reply/add)
  - `comment elect --article-id A --user-id U --yes` / `comment unelect ... --yes` (markelect/unmarkelect, destructive:✔)
  - `comment open --article-id A` / `comment close --article-id A`
  - 复用 `_guarded_call`

- [ ] **Step 1: 写失败测试**

```python
class TestCommentCmd(unittest.TestCase):
    def _run(self, argv, responses):
        fx = FakeUrllibRequest(responses)
        with mock.patch("urllib.request.urlopen", fx), \
             mock.patch.object(wechat_mp, "load_config", return_value=("id", "sec")):
            code = wechat_mp.main(argv)
        return code, fx

    def test_comment_ls(self):
        code, fx = self._run(["comment", "ls", "--article-id", "A1"],
            [{"access_token": "T", "expires_in": 7200}, {"errcode": 0, "comment": []}])
        self.assertEqual(code, 0)
        sent = json.loads(fx.requests[1].data.decode())
        self.assertEqual(sent, {"article_id": "A1", "begin": 0, "count": 50, "type": 0})

    def test_comment_reply(self):
        code, fx = self._run(["comment", "reply", "--article-id", "A1",
                              "--user-id", "U1", "--content", "hi"],
            [{"access_token": "T", "expires_in": 7200}, {"errcode": 0}])
        self.assertEqual(code, 0)
        sent = json.loads(fx.requests[1].data.decode())
        self.assertEqual(sent, {"article_id": "A1", "openid": "U1", "content": "hi"})

    def test_comment_elect_needs_yes(self):
        code, _ = self._run(["comment", "elect", "--article-id", "A1", "--user-id", "U1"],
            [{"access_token": "T", "expires_in": 7200}])
        self.assertEqual(code, 2)
```

- [ ] **Step 2: 运行确认失败** — Expected FAIL

- [ ] **Step 3: 实现**

```python
def cmd_comment(args):
    m = {
        "ls": ("comment_list", {"article_id": args.article_id, "begin": args.begin,
                                 "count": args.count, "type": args.type}),
        "reply": ("comment_reply_add", {"article_id": args.article_id,
                                         "openid": args.user_id, "content": args.content}),
        "elect": ("comment_markelect", {"article_id": args.article_id, "openid": args.user_id}),
        "unelect": ("comment_unmarkelect", {"article_id": args.article_id, "openid": args.user_id}),
        "open": ("comment_open", {"article_id": args.article_id}),
        "close": ("comment_close", {"article_id": args.article_id}),
    }
    if args.action not in m:
        return 2
    endpoint_id, body = m[args.action]
    return _guarded_call(endpoint_id, body, yes=args.yes)[0]
```

`build_parser()` 追加:
```python
    cm = sub.add_parser("comment", help="留言管理")
    cm.add_argument("action", choices=["ls", "reply", "elect", "unelect", "open", "close"])
    cm.add_argument("--article-id", required=True)
    cm.add_argument("--user-id"); cm.add_argument("--content")
    cm.add_argument("--begin", type=int, default=0); cm.add_argument("--count", type=int, default=50)
    cm.add_argument("--type", type=int, default=0)
    cm.add_argument("--yes", action="store_true")
    cm.set_defaults(func=cmd_comment)
```

registry 中 `comment_markelect`/`comment_unmarkelect` 改 `destructive: true`(精选/取消精选改变公开展示)。

- [ ] **Step 4: 测试通过** — Expected PASS

- [ ] **Step 5: Commit**

```bash
git add community/wechat-mp/
git commit -m "feat(wechat-mp): comment convenience commands"
```

---

### Task 16: `publish` 一键发文链路

**Files:**
- Modify: `community/wechat-mp/scripts/wechat_mp.py`
- Test: `community/wechat-mp/tests/test_wechat_mp.py`

**Interfaces:**
- Produces:
  - `wechat_mp.py publish <file.md|file.json> [--cover cover.png] [--title T] [--author A] [--digest D] [--yes]`
    - JSON 输入: 标准 `{"articles":[{...}]}` 结构直接透传 draft/add
    - MD 输入: 解析 frontmatter(简单 `---` 块)+ 正文;正文本地图片 `![](./x.png)` → 逐个 uploadimg 替换为微信 URL;组装 articles
    - 链路: cover 缺失则查 `imgs/cover.png` 再报错 → material_add(type=image) 取 thumb_media_id → draft_add → freepublish_submit → 轮询 freepublish/get (间隔 3s,最多 20 次,publish_state 0=成功) → 打印 article_url
    - `--yes` 控制 freepublish_submit(destructive:✔);缺 `--yes` 在 submit 前打印草稿 media_id 并退出 0,提示加 `--yes` 发布
  - `parse_markdown(md_text) -> tuple[dict, str]` (frontmatter dict + body)
  - `replace_local_images(body, basedir) -> tuple[str, list[dict]]` (新 body + 上传记录)

- [ ] **Step 1: 写失败测试**

```python
class TestPublish(unittest.TestCase):
    def test_parse_markdown_frontmatter(self):
        md = "---\ntitle: T\nauthor: A\n---\n\n# Hello\n\nbody"
        meta, body = wechat_mp.parse_markdown(md)
        self.assertEqual(meta["title"], "T")
        self.assertIn("body", body)
        self.assertNotIn("---", body)

    def test_parse_markdown_no_frontmatter(self):
        meta, body = wechat_mp.parse_markdown("# Hi\nbody")
        self.assertEqual(meta, {})
        self.assertIn("body", body)

    def test_publish_full_chain_with_yes(self):
        fx = FakeUrllibRequest([
            {"access_token": "T", "expires_in": 7200},            # token
            {"errcode": 0, "media_id": "THUMB", "url": "http://c"}, # material_add
            {"errcode": 0, "media_id": "DRAFT"},                  # draft_add
            {"errcode": 0, "publish_id": "P1"},                   # submit
            {"errcode": 0, "publish_state": 0,
             "article_detail": {"item": [{"article_url": "http://final"}]}}])
        with tempfile.TemporaryDirectory() as d:
            art = os.path.join(d, "a.json")
            with open(art, "w") as f:
                f.write(json.dumps({"articles": [{"title": "t", "content": "<p>c</p>",
                                                  "thumb_media_id": "IGNORED", "need_open_comment": 1}]}))
            cov = os.path.join(d, "cover.png")
            with open(cov, "wb") as f:
                f.write(b"\x89PNG")
            with mock.patch("urllib.request.urlopen", fx), \
                 mock.patch.object(wechat_mp, "load_config", return_value=("id", "sec")), \
                 mock.patch.object(wechat_mp.time, "sleep"):
                import io, contextlib
                buf = io.StringIO()
                with contextlib.redirect_stdout(buf):
                    code = wechat_mp.main(["publish", art, "--cover", cov, "--yes"])
        self.assertEqual(code, 0)
        draft_sent = json.loads(fx.requests[3].data.decode())
        self.assertEqual(draft_sent["articles"][0]["thumb_media_id"], "THUMB")
        self.assertIn("http://final", buf.getvalue())

    def test_publish_without_yes_stops_at_draft(self):
        fx = FakeUrllibRequest([
            {"access_token": "T", "expires_in": 7200},
            {"errcode": 0, "media_id": "THUMB", "url": "http://c"},
            {"errcode": 0, "media_id": "DRAFT"}])
        with tempfile.TemporaryDirectory() as d:
            art = os.path.join(d, "a.json")
            with open(art, "w") as f:
                f.write(json.dumps({"articles": [{"title": "t", "content": "c"}]}))
            cov = os.path.join(d, "cover.png")
            with open(cov, "wb") as f:
                f.write(b"\x89PNG")
            with mock.patch("urllib.request.urlopen", fx), \
                 mock.patch.object(wechat_mp, "load_config", return_value=("id", "sec")):
                code = wechat_mp.main(["publish", art, "--cover", cov])
        self.assertEqual(code, 0)
        self.assertEqual(len(fx.requests), 3)  # 未 submit

    def test_publish_requires_cover(self):
        with tempfile.TemporaryDirectory() as d:
            art = os.path.join(d, "a.json")
            with open(art, "w") as f:
                f.write(json.dumps({"articles": [{"title": "t", "content": "c"}]}))
            code = wechat_mp.main(["publish", art])
        self.assertEqual(code, 2)
```

- [ ] **Step 2: 运行确认失败** — Expected FAIL

- [ ] **Step 3: 实现**

```python
def parse_markdown(md_text):
    meta = {}
    body = md_text
    if md_text.startswith("---"):
        parts = md_text.split("---", 2)
        if len(parts) >= 3:
            for line in parts[1].strip().splitlines():
                if ":" in line:
                    k, v = line.split(":", 1)
                    meta[k.strip()] = v.strip()
            body = parts[2].lstrip("\n")
    return meta, body

def _upload_cover(cover_path):
    payload = api_call("POST", "/cgi-bin/material/add_material",
                       extra_query={"type": "image"}, file=_read_file(cover_path))
    if payload.get("errcode", 0) != 0:
        _emit(payload)
        raise SystemExit(1)
    return payload["media_id"]

def _upload_body_images(body, basedir):
    import re
    uploaded = []
    def repl(m):
        alt, path = m.group(1), m.group(2)
        if not re.match(r"^(https?://|/)", path):
            local = os.path.join(basedir, path)
            if os.path.isfile(local):
                up = api_call("POST", "/cgi-bin/media/uploadimg", file=_read_file(local))
                if up.get("errcode", 0) == 0:
                    uploaded.append({"local": path, "url": up["url"]})
                    return f"![{alt}]({up['url']})"
        return m.group(0)
    return re.sub(r"!\[([^\]]*)\]\(([^)]+)\)", repl, body), uploaded

def cmd_publish(args):
    with open(args.file, encoding="utf-8") as f:
        raw = f.read()
    if args.file.endswith(".json"):
        payload = json.loads(raw)
        articles = payload.get("articles")
        if not articles:
            print("json must contain 'articles' array", file=sys.stderr)
            return 2
        body_html = None
    else:
        meta, body = parse_markdown(raw)
        basedir = os.path.dirname(os.path.abspath(args.file))
        body, _imgs = _upload_body_images(body, basedir)
        articles = [{
            "title": args.title or meta.get("title", ""),
            "content": body, "thumb_media_id": "PENDING",
            "need_open_comment": 1, "only_fans_can_comment": 0}]
        if args.author or meta.get("author"):
            articles[0]["author"] = args.author or meta.get("author")
        if args.digest or meta.get("digest"):
            articles[0]["digest"] = args.digest or meta.get("digest")

    cover = args.cover
    if not cover:
        cand = os.path.join(os.path.dirname(os.path.abspath(args.file)), "imgs", "cover.png")
        if os.path.isfile(cand):
            cover = cand
    if not cover:
        print("cover image required: --cover or imgs/cover.png", file=sys.stderr)
        return 2
    thumb_media_id = _upload_cover(cover)
    for a in articles:
        a.setdefault("thumb_media_id", "PENDING")
        if a["thumb_media_id"] == "PENDING":
            a["thumb_media_id"] = thumb_media_id

    resp = api_call("POST", "/cgi-bin/draft/add", body={"articles": articles})
    if resp.get("errcode", 0) != 0:
        _emit(resp)
        return 1
    media_id = resp["media_id"]
    if not args.yes:
        print(json.dumps({"draft_media_id": media_id,
                          "hint": "draft created; run again with --yes to publish"},
                         ensure_ascii=False, indent=2))
        return 0

    sub = api_call("POST", "/cgi-bin/freepublish/submit", body={"media_id": media_id})
    if sub.get("errcode", 0) != 0:
        _emit(sub)
        return 1
    publish_id = sub["publish_id"]
    for _ in range(20):
        time.sleep(3)
        st = api_call("POST", "/cgi-bin/freepublish/get", body={"publish_id": publish_id})
        if st.get("errcode", 0) != 0:
            _emit(st)
            return 1
        state = st.get("publish_state")
        if state == 0:
            urls = [i["article_url"] for i in
                    st.get("article_detail", {}).get("item", []) if i.get("article_url")]
            _emit({"publish_id": publish_id, "state": "success", "article_urls": urls,
                   "draft_media_id": media_id})
            return 0
        if state in (2, 3):  # 原创失败/常规失败
            _emit({"publish_id": publish_id, "state": state, "fail_idx":
                   st.get("fail_idx", []), "raw": st})
            return 1
    _emit({"publish_id": publish_id, "state": "timeout",
           "hint": "still publishing; check later via 'raw POST /cgi-bin/freepublish/get'"})
    return 1
```

`build_parser()` 追加:
```python
    pb = sub.add_parser("publish", help="一键发文: 封面→草稿→发布→轮询")
    pb.add_argument("file", help=".md 或 .json (标准 articles 结构)")
    pb.add_argument("--cover"); pb.add_argument("--title"); pb.add_argument("--author")
    pb.add_argument("--digest"); pb.add_argument("--yes", action="store_true")
    pb.set_defaults(func=cmd_publish)
```

`test_publish_full_chain_with_yes` 中 `time.sleep` 已 mock,轮询立即返回。

- [ ] **Step 4: 测试通过** — Expected PASS (4 tests)

- [ ] **Step 5: Commit**

```bash
git add community/wechat-mp/
git commit -m "feat(wechat-mp): publish pipeline — cover→draft→submit→poll, --yes gates publish"
```

---

### Task 17: SKILL.md 定稿 + references + examples

**Files:**
- Modify: `community/wechat-mp/SKILL.md` (全量重写)
- Create: `community/wechat-mp/references/api-catalog.md`
- Create: `community/wechat-mp/references/errors.md`
- Create: `community/wechat-mp/examples/publish-article.md`
- Create: `community/wechat-mp/examples/manage-drafts.md`

**Interfaces:**
- Consumes: 全部命令已实现 (Task 7-16)
- Produces: 入库级文档

- [ ] **Step 1: 重写 SKILL.md**

结构(完整写出,不含占位):
1. frontmatter: name/description(触发词: 微信公众号、公众号接口、素材、草稿、发布、留言、菜单、群发、数据统计)/version 1.0.0/tags
2. 快速开始: `doctor` → 凭证设置
3. 4 层用法表: raw / list+call / draft·material·comment / publish,每条给 1 个真实命令示例
4. 安全规则: destructive + `--yes`、secret 不落日志
5. 常见 errcode 表(引用 references/errors.md)
6. 不重复 api-catalog 全量内容 — 链接 `references/api-catalog.md`

- [ ] **Step 2: 写 references/api-catalog.md**

按 22 个 category 分节,每节表格列 id/name/method/path/destructive — 内容直接由 `wechat_mp.py list` 输出生成:
Run: `python3 community/wechat-mp/scripts/wechat_mp.py list > /tmp/eps.json` 后转 markdown 表格写入,文首注明"生成自 endpoints.json,改注册表后重新生成"。

- [ ] **Step 3: 写 references/errors.md**

errcode 速查表(-1/40001/40002/40013/40125/40164/40243/41004/45009/50004/50007/-2 本地码),每条: 含义 + 修复动作(45009 → `call quota_clear_all --yes`)。

- [ ] **Step 4: 写 examples**

`examples/publish-article.md`: 从 markdown 文件到发布的完整会话(含 doctor、cover 准备、publish 无 --yes 看 draft、加 --yes 发布、拿 article_url)。
`examples/manage-drafts.md`: draft ls/get/del、material upload/ls/del、comment ls/reply 的命令序列与期望输出片段。

- [ ] **Step 5: 结构校验**

Run: `python3 devops/skill-manager/scripts/scan_and_check.py community/wechat-mp/`
Expected: PASS (SKILL.md frontmatter 合规, examples 非空)

- [ ] **Step 6: Commit**

```bash
git add community/wechat-mp/
git commit -m "docs(wechat-mp): finalize SKILL.md + api-catalog/errors references + examples"
```

---

### Task 18: 全量验证 + arena 入库

**Files:**
- Modify: `skill-arena/*.json`, `index.json` (由 pipeline 生成)

**Interfaces:**
- Consumes: skill 完整实现 (Task 1-17)

- [ ] **Step 1: 全量单测**

Run: `python3 -m unittest discover -s community/wechat-mp/tests -v`
Expected: 全 PASS (~25 tests)

- [ ] **Step 2: CLI 冒烟(离线部分)**

Run:
```bash
python3 community/wechat-mp/scripts/wechat_mp.py list --category 素材管理 | head -3
python3 community/wechat-mp/scripts/wechat_mp.py --help
python3 community/wechat-mp/scripts/wechat_mp.py call draft_delete --data '{}' ; echo "exit=$?"
```
Expected: list 输出 JSON;help 列全部子命令;call draft_delete 无 --yes exit=2

- [ ] **Step 3: arena pipeline 重建 (CLAUDE.md 强制规则)**

Run:
```bash
python3 scripts/arena_scan.py && python3 scripts/arena_cluster_score.py && python3 scripts/arena_build_index.py
```
Expected: 三步全成功,wechat-mp 进入 `skill-arena/skills_inventory.json`

- [ ] **Step 4: hub 搜索验证**

Run: `python3 devops/ultraskills-hub/scripts/search.py 微信公众号 | head -20`
Expected: wechat-mp 出现在结果中 (match_score > 0)

- [ ] **Step 5: 真实账号验收(用户配合)**

- `python3 community/wechat-mp/scripts/wechat_mp.py doctor` → 全 PASS
- `publish examples 样例.md --yes` → 拿到 article_url
- 验收通过后在 SKILL.md 版本号改 1.0.0

- [ ] **Step 6: Commit + 收尾**

```bash
git add community/wechat-mp/ skill-arena/ index.json
git commit -m "feat(wechat-mp): wechat-mp v1.0.0 — full-coverage WeChat MP API skill (116 endpoints)"
```

---

## Self-Review 记录

1. **Spec 覆盖**: L0 (Task 2-4), L1 (Task 5-7), L2 (Task 8-12), L3 (Task 13-16), 安全规则 (Task 8/14/15/16 destructive guard), 测试 (每 task TDD), 验收 (Task 18 + spec §8 全项)。多账号/MCP/browser 明确非目标 ✓
2. **占位符**: 无 TBD/TODO;批量任务给出完整表格数据 ✓
3. **类型一致**: `_guarded_call(endpoint_id, body, file, yes, extra_query)` 在 Task 14 定义、Task 15 复用签名一致;`FakeUrllibRequest` 在 Task 6 定义后被 Task 7/13/14/15/16 复用;`api_call` 的 `extra_query` 参数 Task 6 定义、Task 14/16 使用一致 ✓
